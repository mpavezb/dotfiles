;; -----------------------------------------------------------------------------
;; helm-fd
;; https://github.com/lerouxrgd/helm-fd
;; -----------------------------------------------------------------------------
;; Note: fzf package is faster.
(use-package helm-fd)

;; -----------------------------------------------------------------------------
;; helm-rg
;; https://github.com/cosmicexplorer/helm-rg
;; -----------------------------------------------------------------------------
;; Note: rg package works has better interface.
(use-package helm-rg)

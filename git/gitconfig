# .gitconfig
# Matias Pavez, matias.pavez.b@gmail.com
#

[user]
	name = Matias Pavez
	;email = matias.pavez.b@gmail.com
	email = 14824728+mpavezb@users.noreply.github.com
	; signingkey = KEY-ID

[commit]
	gpgsign = true

[credential]
	helper = cache --timeout=86400

[core]
	autocrlf = input
	safecrlf = true
	editor = emacsclient

[push]
	default = simple

[alias]
	co = checkout
	ci = commit
	st = status
	br = branch

# ============================
# Colors
# ============================
# - colors: [ normal  black  red  green  yellow  blue  magenta  cyan  white ]
# - attrs:  [ bold dim ul blink reverse ]

[color]
	ui = auto
	diff = auto
	status = auto
	branch = auto

[color "branch"]
	current = yellow reverse
	local = yellow 
	remote = green
	plain = normal

[color "status"]
	header = normal
	added = green
	changed = yellow
	untracked = red
	branch = blue bold
	nobranch = red
	unmerged = magenta bold

[color "diff"]
	plain = normal
	meta = yellow
	frag = magenta bold
	old = red bold
	new = green bold
	commit = yellow
	whitespace = normal red
